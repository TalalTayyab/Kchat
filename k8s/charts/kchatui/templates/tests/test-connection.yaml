apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kchatui.fullname" . }}-test-connection"
  labels:
    {{- include "kchatui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kchatui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
