apiVersion: v1
kind: Service
metadata:
  name: {{ include "kchatui.fullname" . }}
  labels:
    {{- include "kchatui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kchatui.selectorLabels" . | nindent 4 }}
